# 🏫 School Life: Open Campus — زندگی در مدرسه

یک بازی **سه‌بعدی جهان‌باز (Open World)** سبک و کاملاً **اورجینال** که مستقیم در مرورگر اجرا می‌شود.
تو یک دانش‌آموز هستی در «دبیرستان آفتاب»: آزادانه بگرد، با بقیه حرف بزن، ۸ مأموریت انجام بده،
با خودروی محوطه رانندگی کن و مدرسه را کشف کن!

> ⚠️ **نکته:** به‌خاطر قوانین امنیتی مرورگر، بازی را نباید با دابل‌کلیک روی `index.html` باز کنی
> (ماژول‌های جاوااسکریپت روی `file://` کار نمی‌کنند). حتماً با یکی از روش‌های زیر یک **سرور ساده** اجرا کن.

---

## ▶️ روش اجرا (ویندوز)

### روش ۱ — راحت‌ترین (دابل‌کلیک)
1. روی فایل **`start.bat`** دابل‌کلیک کن.
2. مرورگر خودکار باز می‌شود و بازی لود می‌شود. ✅

> اگر پایتون نصب نیست، از [python.org](https://www.python.org/downloads/) نصبش کن
> (حین نصب تیک Add to PATH را بزن).

### روش ۲ — با ترمینال
```bash
cd school-life
python -m http.server 8080
```
بعد در مرورگر باز کن: **http://localhost:8080**

### روش ۳ — با Node.js
```bash
cd school-life
npx serve .
```

مرورگرهای پیشنهادی: **Chrome** و **Edge** (آخرین نسخه). نیاز به کیبورد و موس دارد.

---

## 🎮 کنترل بازی

| کلید | عمل |
|---|---|
| `W A S D` | حرکت |
| موس | نگاه کردن (کلیک = قفل موس) |
| `Shift` | دویدن (انرژی مصرف می‌کند) |
| `Space` | پرش / ترمز خودرو |
| `E` | صحبت، برداشتن آیتم، سوار/پیاده شدن |
| `Tab` یا `M` | نقشه بزرگ |
| `I` | کوله‌پشتی |
| `P` یا `Esc` | توقف / منو |
| `۱-۹` | انتخاب گزینه در گفت‌وگو |

حین رانندگی: `W/S` گاز و دنده‌عقب، `A/D` فرمان، `Space` ترمز دستی، `E` پیاده شدن.

---

## 🎯 مأموریت‌ها (۸ مأموریت، همه غیرخشونت‌آمیز)

1. **کوله‌پشتی گمشده** — کوله سارا را پشت سالن ورزشی پیدا کن (دهنده: سارا در حیاط)
2. **کتاب‌های کتابخانه** — ۳ کتاب از قفسه کلاس A به کتابدار برسان (دهنده: آقای فرهادی)
3. **آزمون فوتبال** — ۳ گل به دروازه شرقی بزن (دهنده: مربی رستمی در زمین فوتبال)
4. **نامه فوری** — نامه را در ۱۵۰ ثانیه به دفتر مدیر برسان (دهنده: نیما در راهرو)
5. **مسیر مخفی** — باغ مخفی پشت ساختمان را کشف کن (راهنما: باغبان کریمی)
6. **ناهار کیان** — از بوفه ساندویچ بخر و به کیان بده (دهنده: کیان در حیاط)
7. **قهرمان بازیافت** — ۸ قوطی محوطه را جمع کن (دهنده: دنیا در پارکینگ)
8. **معمای کتابدار** — معمای آقای احمدی را حل کن (کتابخانه)

دنبال NPCهایی با علامت **!** طلایی برو. اهداف روی مینی‌مپ با دایره طلایی چشمک‌زن مشخص‌اند.

---

## ⚙️ امکانات

- 🌍 محوطه باز: ساختمان مدرسه (۲ کلاس + دفتر مدیر + راهرو)، حیاط، فواره، زمین فوتبال،
  سالن ورزشی، کتابخانه، بوفه، پارکینگ، باغ مخفی، خیابان و محله اطراف
- 🌞 چرخه روز و شب (یک شبانه‌روز ≈ ۱۲ دقیقه)
- 🧑‍🎓 ۱۸ NPC با برنامه روزانه (کلاس/تفریح/شب)، گشت‌زنی و گفت‌وگو
- 🚗 ۳ وسیله نقلیه قابل رانندگی + ۱ خودروی قفل معلم
- 💰 سکه، امتیاز، کوله‌پشتی، فروشگاه بوفه، جان و انرژی
- 🗺 مینی‌مپ زنده + نقشه بزرگ + ردیاب مأموریت
- 💾 ذخیره خودکار و دستی با LocalStorage (ادامه بازی)
- 🎨 سه سطح گرافیکی Low/Medium/High + شمارنده FPS
- 🔊 صداهای procedural (بدون هیچ فایل صوتی — ساخته‌شده با WebAudio)

---

## 🚀 انتشار نسخه Static

این پروژه **۱۰۰٪ استاتیک** است (HTML + CSS + JS، بدون بک‌اند) و همین پوشه مستقیم قابل انتشار است:

- **Netlify / Vercel:** همین پوشه `school-life` را درگ‌ان‌دراپ کن.
- **GitHub Pages:** محتویات پوشه را در ریپو/برانچ `gh-pages` بگذار.
- **هاست اشتراکی / IIS:** فایل‌ها را در `public_html` یا `wwwroot` کپی کن.

تنها وابستگی خارجی، کتابخانه **Three.js** است که در مرورگر کاربر از CDN لود می‌شود
(با دو CDN پشتیبان: jsDelivr و unpkg).

### اجرای کاملاً آفلاین (اختیاری)
1. فایل `three.module.js` نسخه `0.160.0` را دانلود کن:
   `https://cdn.jsdelivr.net/npm/three@0.160.0/build/three.module.js`
2. آن را با همین نام در مسیر `school-life/src/lib/three.module.js` بگذار.
3. بازی به‌صورت خودکار اول کپی محلی را امتحان می‌کند و بدون اینترنت هم اجرا می‌شود.

---

## 🧩 ساختار پروژه

```
school-life/
├── index.html          # صفحه اصلی + بوت‌لودر Three.js
├── style.css           # رابط کاربری فارسی
├── start.bat           # اجرای تک‌کلیکی در ویندوز
├── src/
│   ├── main.js         # حلقه بازی، روز/شب، ورودی، ذخیره
│   ├── player.js       # کنترل بازیکن سوم‌شخص + دوربین
│   ├── world.js        # ساخت محوطه، ساختمان‌ها، توپ فوتبال
│   ├── npc.js          # NPCها، برنامه روزانه، مسیریابی
│   ├── vehicles.js     # وسایل نقلیه + فیزیک آرکید
│   ├── missions.js     # ۸ مأموریت + دیالوگ‌ها + فروشگاه
│   ├── ui.js           # HUD، نقشه، منوها، مودال‌ها
│   ├── save.js         # ذخیره/بازیابی LocalStorage
│   ├── settings.js     # تنظیمات گرافیک/صدا
│   ├── audio.js        # صداهای WebAudio
│   ├── gfx.js          # تکسچر procedural + سیستم ذرات (Pool)
│   ├── utils.js        # ریاضیات، فارسی‌سازی، برخورد
│   └── lib/            # (اختیاری) کپی محلی three.module.js
├── assets/
│   ├── models/         # مدل‌ها procedural ساخته می‌شوند (خالی)
│   ├── textures/       # تکسچرها procedural ساخته می‌شوند (خالی)
│   └── audio/          # صداها procedural ساخته می‌شوند (خالی)
└── test/
    └── logic.test.mjs  # تست منطق خالص (node)
```

> همه مدل‌ها، تکسچرها و صداها **در لحظه با کد ساخته می‌شوند** — به همین دلیل حجم پروژه
> زیر ۲۰۰ کیلوبایت است (به‌جز Three.js که از CDN می‌آید).

---

## 🧪 تست سلامت کد

```bash
cd school-life
node test/logic.test.mjs   # تست منطق (برخورد، فارسی‌سازی، ...)
node test/smoke.mjs        # تست دود: اجرای کامل بازی، هر ۸ مأموریت، رانندگی، ذخیره
```

## ❓ عیب‌یابی

| مشکل | راه‌حل |
|---|---|
| صفحه سیاه / لود نشدن | حتماً با سرور اجرا کن، نه دابل‌کلیک روی index.html |
| خطای «موتور سه‌بعدی» | اینترنت را بررسی کن (دانلود اول Three.js از CDN) |
| موس کار نمی‌کند | یک‌بار روی صفحه کلیک کن تا موس قفل شود |
| کند بودن | در تنظیمات کیفیت را روی Low بگذار |
| ذخیره پاک شد | ذخیره در LocalStorage مرورگر است؛ با پاک شدن کش مرورگر پاک می‌شود |
